  <%= form_for(@school) do |f| %>
      <% if @school.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

            <ul>
              <% @school.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <div class="field">
        <%= f.label :person %><br>
        <%= f.collection_select(:person_id, Person.all, :id, :name) %>
      </div>
      <div class="field">
        <%= f.label :title %><br>
        <%= f.text_field :title %>
      </div>
      <div class="field">
        <%= f.label :start_year %><br>
        <%= f.date_select :start_year, start_year: 1900 %>
      </div>
      <div class="field">
        <%= f.label :end_year %><br>
        <%= f.date_select :end_year, start_year: 1900 %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
  <% end %>